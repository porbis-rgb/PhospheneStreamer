kind = "web"
previewPath = "/"
title = "Phosphene Simulator"
version = "1.0.0"

[[services]]
localPort = 8000
name = "Phosphene Simulator"
paths = ["/"]

[services.development]
run = "cd artifacts/phosphene-sim && uvicorn main:app --host 0.0.0.0 --port 8000 --reload"
